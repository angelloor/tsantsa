export interface Schedule {
  id_schedule: string;
  start_date_schedule: string;
  end_date_schedule: string;
  tolerance_schedule: number;
  creation_date_schedule: string;
  deleted_schedule: boolean;
}
